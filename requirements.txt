# Core dependencies
PyQt5>=5.15.0
ffmpeg-python>=0.2.0

# Audio processing
pydub>=0.25.1
librosa>=0.10.0
soundfile>=0.12.0

# AI/ML - Install these separately if needed
funasr>=1.0.0
modelscope
numpy
python-Levenshtein
# torch
# torchaudio

# Note: For SenseVoice, install FunASR after basic setup:
# pip install -U funasr modelscope
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu118
